class PDFHandler:
    def process(self, input_path, output_path, watermark_text):
        # 这里的实现较为复杂，通常涉及 PDF -> Image List -> Watermark -> PDF
        # 为保持 demo 简洁，这里留作接口
        print(f"[!] PDF handling is a future feature. File: {input_path}")
        pass